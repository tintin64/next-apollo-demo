import breakpoints from './break-points';
import containerMaxWidth from './container-max-width';
const theme = {
    breakpoints,
    containerMaxWidth,
    colors: {
        primary: '#0070f3',
    },
};

export default theme;